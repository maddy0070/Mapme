<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      The platform theme exists only to paint the window before Compose takes
      over. Everything visible after that comes from MapMeTheme, not from here.
      A cold start therefore goes ink -> mark -> app, with no white flash.
    -->
    <style name="Theme.MapMe" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/mapme_canvas</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.MapMe.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/mapme_canvas</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_mark</item>
        <item name="postSplashScreenTheme">@style/Theme.MapMe</item>
    </style>
</resources>
